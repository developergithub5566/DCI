@model IEnumerable<DCI.Models.ViewModel.DTRCorrectionViewModel>
@using Newtonsoft.Json;
@using DCI.Core.Common;
@using DCI.Models.ViewModel;

<link href="~/css/dcicustom.css" rel="stylesheet" />



@{
    var modulePageAccess = Context.Session.GetString("ModulePageAccess");
    var canView = false;
    var canAdd = false;
    var canEdit = false;
    var canDelete = false;

    if (modulePageAccess == null)
    {
        <script>
            $.ajax({
            type: 'POST',
            url: '@Url.Action("Logout", "Account")',
            success: function (response) {

            Swal.fire({
            text: "Your session has timed out. Please log in again.",
            icon: "info",
            confirmButtonText: "Ok"
            }).then((result) => {
            window.location.href = '/Account/Logout';
            });
            },
            error: function () {
            Swal.fire({
            text: "An error occurred. Please try again.",
            icon: "error"
            });
            }
            })
        </script>
    }
    else
    {
        var module = JsonConvert.DeserializeObject<List<ModuleInRoleViewModel>>(modulePageAccess)?
                    .FirstOrDefault(x => x.ModulePageId == (int)EnumModulePage.DTRCorrection);
        canView = module?.View ?? false;
        canAdd = module?.Add ?? false;
        canEdit = module?.Update ?? false;
        canDelete = module?.Delete ?? false;
    }

}

<div class="container-fluid">
    <div class="spinner-overlay">
        <div class="spinner-border text-light" role="status">
            <span class="visually-hidden">Loading...</span>
        </div>
    </div>
    <div class="d-sm-flex align-items-center justify-content-between mb-3">
        <h6 class="m-0 font-weight-bold text-gray-800">@ViewBag.BreadCrumbLabel</h6>
        <div class="upperRightDivBtn">
            <a href="#" title="Add DTR Adjustment" class="btn btnPrimary btn-icon-split" data-toggle="modal" data-target="#" onclick="newForm()">
                <span class="icon text-white"><i class="fa-solid fa-plus text-xs"></i></span>
                <span class="text text-white"><small>File DTR Adjustment</small></span>
            </a>
        </div>

    </div>
    <div class="col-12 p0">
        <ul class="breadcrumb">
            <li><a href="~/Home/Index">Dashboard</a></li>
            <li><a href="#">Daily Time Record</a></li>
            <li><a href="#">@ViewBag.BreadCrumbLabel</a></li>
        </ul>
    </div>

    <div class="card shadow mb-4">
        <div class="card-header">
            <label class="mb0">Data</label>
        </div>
        <div class="card-body">
            <div id="accordion">
                <form id="frmFilter" class="row">
                    <div class="form-group col-3">
                        <label class="frmText">Date Covered From: </label>
                        <div class="input-group">

                            <input type="text" name="minDate" id="minDate" class="form-control frmField" autocomplete="off">
                            <div class="input-group-prepend prependBtn" id="calendarIconFrom">
                                <i class="fa fa-calendar"></i>
                            </div>
                            <label for="minDate" generated="true" class="error"></label>
                        </div>
                    </div>
                    <div class="form-group col-3">
                        <label class="frmText">Date Covered To: </label>
                        <div class="input-group">
                            <input type="text" name="maxDate" id="maxDate" class="form-control frmField" autocomplete="off">
                            <div class="input-group-prepend prependBtn" id="calendarIconTo">
                                <i class="fa fa-calendar"></i>
                            </div>
                            <label for="maxDate" generated="true" class="error"></label>
                        </div>
                    </div>
                </form>
            </div>
            <div class="table-responsive mt20">
                <table class="table table-bordered" id="tblDTR">
                    <thead>
                        <tr>


                            <th>
                                <label>Request No</label>
                            </th>
                      
                                <th>
                                    <label>Employee</label>
                                </th>
                           
                           
                            <th>
                                <label>DTR Date & Time</label>
                            </th>
                            <th>
                                <label>Type</label>
                            </th>
                            <th>
                                <label>Status</label>
                            </th>
                            <th>
                                <label>Date Filed</label>
                            </th>
                            <th>
                                Action
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            string statusClass = item.Status switch
                            {
                                (int)EnumStatus.Approved => "btn-primary",
                                (int)EnumStatus.ForApproval => "btn-success",
                                (int)EnumStatus.Rejected => "btn-danger",
                                (int)EnumStatus.Cancelled => "btn-secondary",
                                (int)EnumStatus.VLDeducted
                                or (int)EnumStatus.PayrollDeducted => "btn-warning",
                                _ => "btn-dark"
                            };
                            <tr>
                                <td>
                                  @item.RequestNo
                                  
                                </td>
                             
                                    <td>
                                        @item.EmployeeName
                                    </td>
                              
                              
                                <td>
                                    @item.DtrDateTime.ToString("yyyy-MM-dd HH:mm")                         
                                </td>
                                <td>
                                    @if (item.DtrType == 1)
                                    {
                                        @("Time In");                                        
                                    }
                                    else
                                    {
                                        @("Time Out");                                        
                                    }
                                </td>
                                <td class="text-center">
                                    <strong>
                                        <span class="@statusClass applicationStatus">@item.StatusName</span>
                                    </strong>
                                </td>
                                <td>
                                    @item.DateFiled.ToString("MM/dd/yyyy HH:mm")
                                </td>
                              
                                <td class="text-center">
                                    <a href="javascript:void(0)" onclick="view(@item.DtrId)" title="View" class="btn btn-primary"><i class="fas fa-search"></i></a>
                                    
                                    @if(item.Status  == (int)EnumStatus.ForApproval)
                                    {
                                        <a href="javascript:void(0)" onclick="cancel(@item.DtrId)" title="Cancel Request" class="btn btn-secondary"><i class="fas fa-ban"></i></a>
                                    }
                                   
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
</<div>
</div>


            <div class="modal fade" id="createmodal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true" data-backdrop="static">
                <div class="modal-dialog" role="document">
                    <div class="modal-content">
                        <div class="modal-header">
                            <h5 class="modal-title" id="exampleModalLabel">DTR Adjustment</h5>
                            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                <span aria-hidden="true">&times;</span>
                            </button>
                        </div>
                        <form id="createForm">
                            <div class="modal-body">
                           @*      <table class="viewDetailsTbl table table-striped">
                                    <tbody>
                                       
                                        <tr>
                                            <th>DTR Date:</th>
                                            <td class="tblDate">
                                                <div class="input-group">
                                                    <input type="text" name="COVERED_DATE" id="dateCovered" class="form-control frmField" autocomplete="off">
                                                    <div class="input-group-prepend prependBtn">
                                                        <i class="fa fa-calendar"></i>
                                                    </div>
                                                </div>
                                                <label for="dateCovered" generated="true" class="error"></label>
                                            </td>
                                        </tr>
                                    </tbody>
                                </table> *@

                               @*  <form class="row" id="createForm" method="POST"> *@
                                    <input type="hidden" name="DTR_DATE">
                                    <div class="col-lg-12">
                                        <div class="form-group">
                                            <label class="frmText"><span class="required">*</span>DTR Date</label>
                                            <div class="input-group">
                                                <input type="text" name="dateCovered" id="dateCovered" class="form-control frmField" autocomplete="off" required>
                                                        <div class="input-group-prepend prependBtn" id="calendarIconId">
                                                            <i class="fa fa-calendar"></i>
                                                        </div>
                                            </div>
                                            <label for="dateCovered" generated="true" class="error"></label>
                                        </div>                                 
                                    </div>

                                <div class="col-lg-12">
                                    <div class="form-group">
                                        <label class="frmText"><span class="required">*</span>DTR Time</label>
                                        <div class="input-group clockpicker" data-autoclose="true">
                                            <input type="text" name="dtrTime" id="dtrTime" class="form-control frmField" autocomplete="off" readonly placeholder="24-Hour Format">
                                            <div class="input-group-prepend prependBtn" id="calendarIconOTTo">
                                                <i class="fa fa-calendar"></i>
                                            </div>
                                        </div>
                                 
                                        <label for="dtrTime" generated="true" class="error"></label>
                                    </div>
                                </div>
                            
                                    <div class="col-lg-6">
                                        <div class="form-group">
                                            <label class="frmText"><span class="required">*</span> Type</label>
                                            <select class="form-control frmField" name="DtrType" id="DtrType" required>
                                            <option value="" disabled selected>Select</option>
                                                <option value="1" class="TIME_IN">Time In</option>
                                                <option value="2" class="TIME_OUT">Time Out</option>
                                            </select>
                                        </div>
                                    </div>
                             

                                    <div class="col-12">
                                        <div class="form-group">
                                        <label class="frmText"><span class="required">*</span> Reason:</label>
                                            <textarea class="form-control frmField" name="Reason" id="Reason" required></textarea>
                                        </div>
                                    </div>
                        
                                    <div class="col-12">
                                        <hr>

                                    <label class="frmText" id="ApproverLabel" name="ApproverLabel"> </label><br>
                                    <label class="font-weight-bold mb0 text-danger" id="deptHeadName" name="deptHeadName"></label>
                                    <label class="d-block mt0 text-left text-xs" id="ApprovalDate" name="ApprovalDate"></label>
                                    <label class="d-block mt0 text-left text-xs" id="ApprovalRemarks" name="ApprovalRemarks"></label>
                                 
                                    </div>
                         
                            </div>

                            <!-- Modal footer -->
                            <div class="modal-footer pt0">
                           @*      <button class="btn btnPrimary btn-icon-split d-block" onclick="save()" type="button" id="btnSave">
                                    <span class="icon text-white">
                                        <i class="fa-regular fa-file-lines text-xs"></i>
                                    </span>
                                    <span class="text text-white"><small>Submit </small></span>
                                </button> *@
                                <button class="btn btnPrimary btn-icon-split d-block" type="submit" id="btnSave">
                                    <span class="icon text-white"><i class="fa fa-paper-plane" aria-hidden="true"></i></span>
                                    <span class="text text-white"><small>Submit</small></span>
                                </button>

                                <button class="btn btnSecondary btn-icon-split d-block" data-dismiss="modal">
                                    <span class="icon text-white">
                                        <i class="fa-solid fa-xmark text-xs"></i>
                                    </span>
                                    <span class="text text-white"><small>Close </small></span>
                                </button>
                            </div>
                   
                        </form>
                    </div>
                </div>
            </div>

@section scripts {

                <link href="~/js/clockpicker/src/clockpicker.css" rel="stylesheet" />
                <script src="~/js/clockpicker/src/clockpicker.js"></script>
          
                <script type="text/javascript">
                        var js = jQuery.noConflict(true);

                                $('.spinner-overlay').show();

                    setTimeout(() => {
                        $('.spinner-overlay').hide();
                    }, 200);


                        $(document).ready(function () {
                             js('.clockpicker').clockpicker();

                             

                                   $('#calendarIconId').on('click', function () {
                                      $('#dateCovered').trigger('click');
                                    });
                                                     $('#calendarIconFrom').on('click', function () {
                                                                   $('#minDate').trigger('click');
                                                        });
                                      $('#calendarIconTo').on('click', function () {
                                               $('#maxDate').trigger('click');
                                    });


                                            flatpickr("#minDate", {
                                   dateFormat: "Y-m-d",
                                     defaultDate: (function () {
                                    let d = new Date();
                                    d.setMonth(d.getMonth() - 1);
                                    return d;
                                     })()
                                });

                                  flatpickr("#maxDate", {
                                   dateFormat: "Y-m-d",
                                   defaultDate: new Date()
                                });
                               
                                let min = null;
                                let max = null;

                                    // Create custom filtering function
                               js.fn.dataTable.ext.search.push(function (settings, data) {
                                     min = $('#minDate').val();   // e.g. "2025-09-01"
                                     max = $('#maxDate').val();   // e.g. "2025-09-10"
                                    let rawDateTime = (data[2] || '').trim(); // adjust index to your date column
                                

                                    if (!rawDateTime) return false;

                                    // Parse row datetime
                                    let rowDate = new Date(rawDateTime.replace(' ', 'T'));
                           

                                    // Parse min/max (flatpickr gives YYYY-MM-DD)
                                    let minDate = min ? new Date(min + "T00:00:00") : null;
                                    let maxDate = max ? new Date(max + "T23:59:59") : null;

                                    if (minDate && rowDate < minDate) return false;
                                    if (maxDate && rowDate > maxDate) return false;
                                     
                                    return true;
                                });

                         let table =       js('#tblDTR').DataTable({
                                      dom: '<"d-flex flex-column"<"d-flex justify-content-between align-items-center mb-2"B><"d-flex justify-content-between align-items-center"l<f>>>rtip',
                         buttons: [                          
                             {
                                 extend: 'pdf',
                                  text: 'Export to PDF',
                                  title: 'DTR Adjustment Report',
                                 exportOptions: {
                                     columns: ':not(:last-child)' // Excludes the last column
                                 },
                             customize: function (doc) {
                                                                   doc.watermark = {
                                                                    text: 'Property of DBP Data Center, Inc.',   // watermark text
                                                                    color: 'red',
                                                                    opacity: 0.1,
                                                                    bold: true,
                                                                    italics: false
                                                                };

                                                                doc.content.splice(1, 0,
                                                                    {
                                                                        text: 'Period: ' + min + " to " + max,
                                                                        alignment: 'center',
                                                                        margin: [0, 0, 0, 4], // tighter spacing
                                                                        fontSize: 10,
                                                                        italics: true
                                                                    },
                                                                    {
                                                                        text: 'Prepared by: ' + '@Html.Raw(ViewBag.Fullname)', // dynamic from Razor
                                                                        alignment: 'center',
                                                                        margin: [0, 0, 0, 12],
                                                                        fontSize: 10,
                                                                        bold: true
                                                                    }
                                                                          );
                                                                // Add "Date Printed" at bottom-right
                                                                var now = new Date();
                                                                var jsDate = now.toLocaleString(); // e.g. 9/21/2025, 3:25:10 PM

                                                                doc['footer'] = function (currentPage, pageCount) {
                                                                    return {
                                                                        columns: [
                                                                            { text: 'Date Printed: ' + jsDate, alignment: 'left', margin: [20, 0] },
                                                                            { text: 'Page ' + currentPage.toString() + ' of ' + pageCount, alignment: 'right', margin: [0, 0, 20, 0] }
                                                                        ],
                                                                        margin: [20, 0]
                                                                    };
                                                                };
                                                            }
                                                        }
                                                    ],
                         lengthMenu: [ [10, 25, 50, -1], [10, 25, 50, "All"] ], // Entries per page
                         pageLength: 10 ,// Default entries per page
                                 order: [[2, 'desc']]
                            });
                              // Event listener for date inputs
                        $('#minDate, #maxDate').on('change', function () {
                            table.draw();
                        });



                                     flatpickr("#dateCovered", {
                                        enableTime: false,
                                       dateFormat: "Y-m-d",                                     
                                             time_24hr: true,
                                                 maxDate: new Date().setHours(23, 59, 59, 999), // end of today
                                               onChange: function(selectedDates, dateStr, instance) {
                                                     instance._input.setAttribute("required", "required");
                                              }
                                    });

                                    $('#createForm').submit(function (event) {
                                                    event.preventDefault();

                                                    // var _dtrDate = $('#DtrType').val().replace(/\//g, '-');
                                                    // var _dtrTime = $('#dtrTime').val();

                                                    //              if (_dtrTime.length === 5) {
                                                    //                 _dtrTime += ":00";
                                                    //             }
                                                           
                                                    //        var dateParts = _dtrDate.split('-');      // ["2025","12","09"]
                                                    //         var timeParts = _dtrTime.split(':');      // ["13","10"]

                                                    //         var _dateTime = new Date(
                                                    //             parseInt(dateParts[0]),          // year
                                                    //             parseInt(dateParts[1]) - 1,      // month index
                                                    //             parseInt(dateParts[2]),          // day
                                                    //             parseInt(timeParts[0]),          // hour
                                                    //             parseInt(timeParts[1]),          // minute
                                                    //             0                                // seconds
                                                    //         );

                                                        const formData = new FormData();
                                                      formData.append('DtrType', $('#DtrType').val());
                                                       formData.append('DtrDateTime', $('#dateCovered').val());
                                                      formData.append('TimeIn', $('#dtrTime').val());
                                                        formData.append('Reason', $('#Reason').val());



                                                        // console.log($('#dateCovered').val())

                                                        // console.log($('#dtrTime').val())

                                                        // console.log(_dateTime)

                                                               Swal.fire({
                                                                   text: "Are you sure you want to submit this record?",
                                                                   icon: "question",
                                                                   showCancelButton: true,
                                                                   confirmButtonText: "Ok"
                                                               }).then((result) => {
                                                                   if (result.isConfirmed) {
                                                                       $('.spinner-overlay').show();

                                                                       $.ajax({
                                                                           type: 'POST',
                                                                           url: '@Url.Action("SaveDTRCorrection", "DailyTimeRecord")',
                                                                           data: formData,
                                                                               processData: false,
                                                                                  contentType: false,
                                                                           success: function (response) {
                                                                               if (response.success) {
                                                                                   $('#createForm')[0].reset();
                                                                                   js('#createmodal').modal('hide');

                                                                                   Swal.fire({
                                                                                       text: response.message,
                                                                                       icon: "success",
                                                                                       confirmButtonText: "Ok"
                                                                                   }).then((result) => {
                                                                                       location.reload();
                                                                                   });

                                                                               } else {
                                                                                   Swal.fire({
                                                                                       text: response.message,
                                                                                       icon: "error"
                                                                                   });
                                                                               }
                                                                               $('.spinner-overlay').hide();
                                                                           },
                                                                           error: function () {
                                                                               $('.spinner-overlay').hide();
                                                                               Swal.fire({
                                                                                   text: "An error occurred. Please try again.",
                                                                                   icon: "error"
                                                                               });
                                                                           }
                                                                       });
                                                                   }
                                                               });
                                                      });// submit
                  
                        });


                        function newForm(dtr) {
                                             const my_fp = document.querySelector("#dateCovered")._flatpickr;
                                                 my_fp.clear();
                                                   $('#Reason').val('');
                                                                    $('#dtrTime').val("");

                                                    document.getElementById('deptHeadName').textContent = @Html.Raw(JsonConvert.SerializeObject(ViewBag.ApproverHead));                                                       
                                                       document.getElementById('ApproverLabel').textContent = 'For Approval of:';
                                           



                                                                       $('#btnSave').css("visibility", "visible");
                                                         $('#createForm').find('input, select, textarea').prop('disabled', false);
                                js('#createmodal').modal('show');
                        }


                              function view(dtrId) {
                                      event.preventDefault();
                                 
                                       

                                      $.ajax({
                                          type: 'POST',
                                          url: '@Url.Action("DTRCorrectionById", "DailyTimeRecord")',
                                           data: { DtrId: dtrId },
                                          success: function (response) {
                                              if (response.success) {
                                                     console.log(response);
                                                         $('#dateCovered').val(response.data.dtrDateTime);
                                                          $('#DtrType').val(response.data.dtrType);
                                                          $('#Reason').val(response.data.reason);
                                                          
                                                            
                                                     document.getElementById('ApproverLabel').textContent = '';
                                                    document.getElementById('ApprovalDate').textContent = '';
                                                   document.getElementById('ApprovalRemarks').textContent = '';
                                                     document.getElementById('deptHeadName').textContent = '';
                                                        $('#btnSave').css("visibility", "hidden");


                                                        if(response.data.status == @((int)EnumStatus.ForApproval)){  //forapproval
                                                     document.getElementById('deptHeadName').textContent = response.data.departmentHead;
                                                       document.getElementById('ApproverLabel').textContent = 'For Approval of:';
                                              }

                                             else  if(response.data.status == @((int)EnumStatus.Approved)){  //approved                                              
                                                      
                                                       document.getElementById('deptHeadName').textContent = response.data.departmentHead;
                                                      document.getElementById('ApproverLabel').textContent = "Approved By: "; 
                                                       document.getElementById('ApprovalDate').textContent = 'Approved Date and Time: ' + response.data.dateApprovedDisapproved;
                                              }
                                                    else if(response.data.status == @((int)EnumStatus.Rejected) ){     //Rejected
                                                          document.getElementById('deptHeadName').textContent = response.data.departmentHead;
                                                            document.getElementById('ApproverLabel').textContent = 'Rejected By:';                                                           
                                                   document.getElementById('ApprovalRemarks').textContent = 'Remarks: ' + response.data.approvalRemarks;
                                                       document.getElementById('ApprovalDate').textContent = 'Rejected Date and Time:: ' + response.data.dateApprovedDisapproved;

                                               }  else if(response.data.status == @((int)EnumStatus.Cancelled) && response.data.dateModified != null){     //canclled
                                                            let iso =  response.data.dateModified;
                                                       let formatted = iso.split(".")[0].replace("T", " ").slice(0, 16);
                                                      document.getElementById('ApprovalDate').textContent = 'Cancelled Date: ' + formatted;
                                                          
                                             }


                                                             $('#createForm').find('input, select, textarea').prop('disabled', true);
                                                        js('#createmodal').modal('show');
                                              } else {
                                                  Swal.fire({
                                                      text: response.message,
                                                      icon: "error"
                                                  });
                                              }
                                              $('.spinner-overlay').hide();
                                          },
                                          error: function () {
                                              $('.spinner-overlay').hide();
                                              Swal.fire({
                                                  text: "An error occurred. Please try again.",
                                                  icon: "error"
                                              });
                                          }
                                      });
                           
                    }

                    function cancel(id){
                                Swal.fire({
                                    text: "Are you sure you want to cancel this request?",
                                    icon: "warning",
                                    showCancelButton: true,
                                    confirmButtonText: "Yes, cancel it!"
                                }).then((result) => {
                                    if (result.isConfirmed) {
                                        $.ajax({
                                            type: 'POST',
                                            url: '@Url.Action("CancelDTRCorrection", "DailyTimeRecord")',
                                            data: { DTRId: id },
                                            success: function (response) {
                                                if (response.success) {
                                                    Swal.fire({
                                                        text: response.message,
                                                        icon: "success",
                                                        confirmButtonText: "Ok"
                                                    }).then((result) => {
                                                        location.reload();
                                                    });
                                                } else {
                                                    Swal.fire({
                                                        text: response.message,
                                                        icon: "error"
                                                    });
                                                }
                                            },
                                            error: function () {
                                                Swal.fire({
                                                    text: "An error occurred. Please try again.",
                                                    icon: "error"
                                                });
                                            }
                                        });
                                    }
                                });
                    }


                    //        function save() {
                    //             event.preventDefault();
                    //         //     var datecovered =  $('#dateCovered').val();
                    //         //     var dtrtype =   $('#DtrType').val(response.data.dtrType);
                    //         //     var reason = $('#Reason').val(response.data.reason);

                    //         // console.log(datecovered)
                    //         // console.log(dtrtype)
                    //         // console.log(reason)


                    //            const formData = new FormData();                       
                    //               formData.append('DtrType', $('#DtrType').val());
                    //                formData.append('DtrDateTime', $('#dateCovered').val());
                    //                 formData.append('Reason', $('#Reason').val());

                    //                 console.log(formData)

                    //    Swal.fire({
                    //        text: "Are you sure you want to submit this record?",
                    //        icon: "question",
                    //        showCancelButton: true,
                    //        confirmButtonText: "Ok"
                    //    }).then((result) => {
                    //        if (result.isConfirmed) {
                    //            $('.spinner-overlay').show();

                    //            $.ajax({
                    //                type: 'POST',
                    //                url: '@Url.Action("SaveDTRCorrection", "DailyTimeRecord")',
                    //                data: formData,
                    //                    processData: false,
                    //                       contentType: false,
                    //                success: function (response) {
                    //                    if (response.success) {
                    //                        $('#createForm')[0].reset();
                    //                        js('#createmodal').modal('hide');

                    //                        Swal.fire({
                    //                            text: response.message,
                    //                            icon: "success",
                    //                            confirmButtonText: "Ok"
                    //                        }).then((result) => {
                    //                            location.reload();
                    //                        });

                    //                    } else {
                    //                        Swal.fire({
                    //                            text: response.message,
                    //                            icon: "error"
                    //                        });
                    //                    }
                    //                    $('.spinner-overlay').hide();
                    //                },
                    //                error: function () {
                    //                    $('.spinner-overlay').hide();
                    //                    Swal.fire({
                    //                        text: "An error occurred. Please try again.",
                    //                        icon: "error"
                    //                    });
                    //                }
                    //            });
                    //        }
                    //    });

                    // }



                </script>
}
